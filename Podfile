source 'https://github.com/CocoaPods/Specs.git'

platform :ios, '8.1'
use_frameworks!

# ignore all warnings from all pods
inhibit_all_warnings!

target 'MultiFetchedResultsTests' do
    pod 'Expecta'
    pod 'OCMock'
end
